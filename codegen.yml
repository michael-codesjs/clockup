overwrite: true
schema:
  - schema.graphql
  - appsync.types.graphql
generates:
  './types/api.ts':
    plugins:
      - '@graphql-codegen/typescript'
      - typescript-operations
      - typescript-resolvers
      - typescript-validation-schema
    config:
      schema: zod
      notAllowEmptyString: true
